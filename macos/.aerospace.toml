start-at-login = false

accordion-padding = 40

default-root-container-layout = 'accordion'
default-root-container-orientation = 'auto'

[mode.main.binding]

    # cmd-ctrl as common prefix for all aerospace commands
    # ctrl-alt is used for rectangle

    # navigate
    cmd-ctrl-h = 'focus left --boundaries all-monitors-outer-frame --boundaries-action wrap-around-all-monitors'
    cmd-ctrl-j = 'focus down --boundaries all-monitors-outer-frame --boundaries-action wrap-around-all-monitors'
    cmd-ctrl-k = 'focus up --boundaries all-monitors-outer-frame --boundaries-action wrap-around-all-monitors'
    cmd-ctrl-l = 'focus right --boundaries all-monitors-outer-frame --boundaries-action wrap-around-all-monitors'

    # move window
    cmd-ctrl-shift-h = 'move left'
    cmd-ctrl-shift-j = 'move down'
    cmd-ctrl-shift-k = 'move up'
    cmd-ctrl-shift-l = 'move right'

    cmd-ctrl-shift-y = 'join-with left'
    cmd-ctrl-shift-u = 'join-with down'
    cmd-ctrl-shift-i = 'join-with up'
    cmd-ctrl-shift-o = 'join-with right'

    # resize
    cmd-ctrl-minus = 'resize smart -80'
    cmd-ctrl-equal = 'resize smart +80'
    cmd-ctrl-shift-minus = 'resize smart-opposite -80'
    cmd-ctrl-shift-equal = 'resize smart-opposite +80'
    cmd-ctrl-enter = 'fullscreen'
    cmd-ctrl-slash = 'layout accordion tiles'
    cmd-ctrl-c = 'layout floating tiling'

    # multipler monitor
    # I should rarely use these because aerospace is only for single monitor setup

    #cmd-ctrl-leftSquareBracket = 'workspace --wrap-around prev'
    #cmd-ctrl-rightSquareBracket = 'workspace --wrap-around next'

    #cmd-ctrl-alt-leftSquareBracket = 'move-node-to-monitor --focus-follows-window --wrap-around prev'
    #cmd-ctrl-alt-rightSquareBracket = 'move-node-to-monitor --focus-follows-window --wrap-around next'
